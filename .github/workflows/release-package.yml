name: Release Package

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Build and Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN
          settings-path: ${{ github.workspace }}

      - name: Build with Maven
        run: mvn -U -B -ntp package

      - name: Resolve artifact name
        run: echo "TARGET_NAME=$(mvn -q -DforceStdout help:evaluate -Dexpression=project.build.finalName)" >> "$GITHUB_ENV"

      - name: Upload distributable
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: target/${{ env.TARGET_NAME }}.jar

      - name: Publish to GitHub Packages (Maven)
        run: mvn -U -B -ntp deploy -s "$GITHUB_WORKSPACE/settings.xml"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: ProxyFeatures ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: target/${{ env.TARGET_NAME }}.jar
